name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHONUNBUFFERED: "1"
  UV_FROZEN: "1"

jobs:
  lint:
    name: Lint, format, and pre-commit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up environment
        uses: ./.github/actions/setup
        with:
          python-version: "3.11"
          install-system-libs: "false"

      - name: Run pre-commit
        run: uv run --only-group dev pre-commit run --all-files --show-diff-on-failure

  tests:
    name: Python ${{ matrix.python-version }} unit tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up environment
        uses: ./.github/actions/setup
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run unit tests
        run: uv run python -m pytest -q

  pr-integration:
    name: PR integration smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    if: github.event_name == 'pull_request'
    needs: lint

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up environment
        uses: ./.github/actions/setup
        with:
          python-version: "3.11"

      - name: Generate synthetic integration clips
        run: |
          uv run python scripts/make_test_clips.py \
            --out runs/pr-integration/clips \
            --size 160x120 \
            --fps 10 \
            --seconds 1 \
            --which all

      - name: Validate generated clips and write report
        run: |
          uv run python - <<'PY'
          import json
          from pathlib import Path

          import cv2

          clip_dir = Path("runs/pr-integration/clips")
          report = {"clips": []}
          for path in sorted(clip_dir.glob("*.mp4")):
              cap = cv2.VideoCapture(str(path))
              opened = bool(cap.isOpened())
              frames = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0)
              width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH) or 0)
              height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT) or 0)
              cap.release()
              item = {
                  "path": str(path),
                  "bytes": path.stat().st_size,
                  "opened": opened,
                  "frames": frames,
                  "width": width,
                  "height": height,
              }
              report["clips"].append(item)

              if not opened:
                  raise SystemExit(f"OpenCV could not open {path}")
              if path.stat().st_size <= 1024:
                  raise SystemExit(f"{path} is too small to be a valid mp4")
              if not 8 <= frames <= 10:
                  raise SystemExit(f"{path} expected about 10 frames, got {frames}")
              if (width, height) != (160, 120):
                  raise SystemExit(f"{path} expected 160x120, got {width}x{height}")

          expected = {"falldown_test.mp4", "fire_test.mp4", "smoke_test.mp4", "weapon_test.mp4"}
          found = {Path(item["path"]).name for item in report["clips"]}
          if found != expected:
              raise SystemExit(f"expected {sorted(expected)}, got {sorted(found)}")

          out = Path("runs/pr-integration/report.json")
          out.write_text(json.dumps(report, indent=2), encoding="utf-8")
          print(json.dumps(report, indent=2))
          PY

      - name: Upload integration artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr-integration-${{ github.event.pull_request.number }}
          path: runs/pr-integration/
          if-no-files-found: error
